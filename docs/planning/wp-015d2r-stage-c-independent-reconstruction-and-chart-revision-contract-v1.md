# WP-015D2R Stage C Independent Reconstruction And Chart Revision Contract v1

Status: authorized source contract; Stage C reconstruction only; successor execution closed
Parent: `WP-015D2R` / `WPV4-NAVIGATOR-01B`
Support evidence: `WP-015D2U`
Machine registration: `analysis/crpm_world/navigation/wp-015d2r-stage-c-reconstruction-registration-v1.json`
Entry HEAD: `6e47d42b691effe5d34c5c050302def40ae40181`

## 1. Authorized milestone and first stop

This contract authorizes only WP-015D2R Stage C: an independent reconstruction
of the committed Stage B result, repair of the post-execution witness/re-entry
state, and a source-bound revision of the Stage A candidate chart. It does not
authorize another observation run.

The pass uses four durable stages: source; separately committed result/report;
post-result review return; and final work-evidence closeout. The result may derive
semantic reconstruction readiness, but must leave navigator admission pending
and Tau candidate/unlicensed. Only the review return, created after the committed
result/range/parity and three-role synthesis are audited, may admit bounded
re-entry and narrowly license Tau. The exact stop statement is:

> WP-015D2R Stage C stopped; no successor world execution opened.

No Stage B rerun, route enumeration, authority call, calibration replay, D2Q
replay, player observation, timing study, gameplay change, product decision,
mathematical placement, P5 work, or successor-world execution is part of this
contract.

## 2. Exact entry and immutable predecessors

Stage C is rebound to these exact commits:

| Role | Commit | Tree |
| --- | --- | --- |
| Stage B preregistration source | `65b2153714f237964354da3791fe4af4694a0594` | `4f0cc39665cd903c279fa5e9f415120edba53a97` |
| Stage B preregistration terminal | `6e88418a6a90281976dd5f6dcf2e6db55d9e4009` | `70a8c35a2fe102837577883c6dcc0581dce9e2b9` |
| Stage B execution source | `439bba3d92a4b4ab3216ad3fa987e90b983dd2c0` | `aab7b66ea4646c557b31542347ae1f693463d8ae` |
| Stage B result | `d340a2286598a1fd399dfa23ba55426185f0e96d` | `8362b60ca2d33225dc9c7254ca70ec5bb73b1b65` |
| Stage B terminal evidence / Stage C entry | `6e47d42b691effe5d34c5c050302def40ae40181` | `098d17d9d2598406a0c1a9222c07bd94f5c29aab` |

The Stage B semantic result digest is
`db073dfe67e0687253fe284832b45fad051b463edfd2e8ac9d3fb862b86c3a11`.
The durable result file at the result commit has blob
`2452a19b700fd6c6965fdb20a27824cf386070c9` and Git-byte SHA-256
`947e951f1f155931d066322df8e7b1748c282b812dca8355bf31d08be23f19ff`.

The machine registration binds mode, blob, and Git-byte SHA-256 for fifteen
recoverable predecessor identities: the Stage B contract, registration,
preregistration evidence, primary builder, runner, test, result, report and
terminal evidence; the Stage A contract, chart and review return; the D2Q return;
the exact thin-cut source; and the generic canonicalization utility. Those rows
are normative. A working-tree file may not substitute for the bound Git object.

The Stage A chart and every Stage A, Stage B, D2P, D2Q, cut, schema, authority,
adapter, sealed-executor, gameplay, dependency, CRPM and product byte remain
immutable. The Stage C chart revision is a new result, not an in-place rewrite.

## 3. Pilot and governance frame

`pilot_activation: required`. This pass can accept a material cross-world
return/re-entry and revise a carrier chart. Before governing synthesis, one
read-only `crpm_route_tracer`, one `crpm_covariance_auditor`, and one
`crpm_reentry_reviewer` must inspect the same fixed source boundary. Their
agreement is correlated analysis over shared facts, not empirical independence
or a majority vote.

Every role return must bind current HEAD and exact sources. Its structured
`boundIdentities` must include the source commit/tree/source digest; result
commit/tree; result and report mode/blob/Git-byte SHA-256; semantic, parity,
source-to-result-range and entry-to-result-range digests; and the immutable
Stage B result commit/digest. Governing-synthesis identities cannot substitute
for these per-role bindings. Every return must also state claim status,
support overlap and evidence-independence class; distinguish preserved,
forgotten, newly visible and residual fields; expose disagreements and
false-promotion risks; give reopening conditions; and report HT10/HT11 status.
The governing synthesis in the review return must record current HEAD/source
basis, activation reason, level, cut/stance, protected family, fixed frame,
per-role findings and covariance, historical-status drift, exact committed-
composition status, field survival, disagreements, HT10/HT11 obligations,
stop/reopen decisions for the projected readout, P3, HT8, learned-support
formation and successor world, and the strongest licensed claim plus explicit
non-claims. No source or result artifact may self-attest this later review.

The level is L4+. The cut/stance is equality under
`thin_visible_duel_v0@2` for Stage B source classes, while the immutable D2Q
`authority_v4@2` result remains a separate command-gate anchor chart. The fixed
protected family is:

- recoverable D2Q actor/`expectedTurn` and command-gate distinctions;
- the complete registered one-next-command target over the 274 eligible Stage B
  endpoint occurrences;
- route and exact-state provenance sufficient for audit re-entry without being
  promoted to live recursive state; and
- explicit residue, reopening conditions, and a successor stop.

At entry HEAD there is no exact committed or sealed Stage C composition. The
strongest preimplementation claim is `support_qualified`. Historical Stage B
status is not silently rewritten: it correctly records `W` absent,
`TAU-WPV4-RETURN-01A` candidate/unlicensed, and Stage C closed at that earlier
boundary.

HT10 opens only the bounded independent reconstruction described here. HT11
transfer-back remains stopped until every exact Tau obligation passes, and a pass
licenses only bounded Stage C re-entry. Thin-only completeness, positive history
recovery beyond provenance, P3, HT8 promotion, learned-support formation, and a
successor world stay closed.

### Actual entry-HEAD pilot return

The required entry audit completed before source edits. All three roles bound
Worms HEAD `6e47d42b691effe5d34c5c050302def40ae40181`, tree
`098d17d9d2598406a0c1a9222c07bd94f5c29aab`; CRPM method HEAD
`8c64353f4c8f5504af3e30ca175d9422ca7ae608`, tree
`2f12aa11b796231a33540ca02b6081035b6e1751`; and Stage B result
`d340a2286598a1fd399dfa23ba55426185f0e96d`, digest
`db073dfe67e0687253fe284832b45fad051b463edfd2e8ac9d3fb862b86c3a11`.

| Task / role | Reviewer-authored entry finding | Preserved scope warning |
| --- | --- | --- |
| `/root/stage_c_route_trace` / `crpm_route_tracer` | Independently parsed committed result bytes with no builder or authority; matched 274 endpoints, 9/7 classes, all 7,378 identities, outcomes `0/1099/14/2983/3282`, exact cross-tab, all 1,113 target-relevant pairs unequal pre-budget, and zero same-depth/equal-pre-budget target-relevant pairs. `Q_support` had 6,265 equal pairs and zero `K_target` failures; thin-plus-admissibility had 6,279 equal pairs, zero command failures and 14 continuation failures. W may be present, J is complete only for `K_target`, and Tau remains unlicensed at entry. | The thin cut stays incomplete; there is no recursive, all-seed, wider-domain or global claim. |
| `/root/stage_c_covariance` / `crpm_covariance_auditor` | Reproduced the same facts and source/result digests via an independent implementation. Implementation paths are `reconstruction_independent`, while every atomic mathematical fact and all reviewer agreement are `correlated_reuse`; there is no empirically independent pair. Depth/budget/admissibility are perfectly co-formed as `2 -> 48 -> 40 -> accepted`, `4 -> 32 -> 24 -> accepted`, and `8 -> 0 -> 0 -> rejected`. `Q_support` is bounded and nonunique; roles stay split; no Stage C artifact exists at entry and the strongest claim is `support_qualified`. | Co-formation and agreement do not promote causation, minimality, evidence weight or placement. |
| `/root/stage_c_reentry` / `crpm_reentry_reviewer` | Independently matched the result and exposed the false-promotion guards: `K_target` excludes exact state/history while 2,983 Q-support-equal pairs retain provenance differences; thin-plus-depth induces the same finite partition; the audit has 15 exact-digest convergences, 2,968 persistent digest differences and 3,282 negative history-recovery controls. Tau/landfall require a committed reviewed return, one command is not recursive update, and agreement adds no empirical weight. It recommended only a separately preregistered longer-horizon successor-congruence test, with no execution. | Exact provenance and finite target sufficiency do not establish admission, recursion, global carrier status or landfall. |

There was no substantive disagreement. One useful wording difference is
preserved: `bounded live carrier` versus `candidate live coordinate`. This
contract resolves it as a target-relative, nonunique `live_carrier` role for
pre-command `movementRemaining` only inside the exact finite `K_target` frame.
It is not public, global, uniquely minimal or recursive.

The combined support is `correlated_reuse`; independent implementation routes
improve falsifiability and recoverability only. Stage A/B closed and W-absent
statuses remain valid historical stops. At entry, the preserved field is the D2Q
core and exact provenance; thin forgets budget, revision, history and exact
state; newly visible facts are the alias/budget splits and nonunique bounded
candidate; residue is longer horizon, wider domains, public formation,
causation/minimality and recursive closure. HT10 is opened only for bounded
reconstruction. HT11 remains stopped pending durable result and review; P3, HT8
beyond provenance, and learned formation stay closed. Reopening requires a new
preregistration or explicit source/cut/frame/target/domain/horizon change.

The strongest entry claim is `support_qualified`: the committed Stage B result
supports a bounded independent reconstruction and candidate thin-plus-pre-budget
sufficiency test. It does not admit navigator re-entry or license Tau. Explicit
non-claims include empirical independence, causation, global/unique minimality,
all-seed/wider-domain or recursive closure, public formation, landfall,
ProductAuthority, mathematical placement, P5 and successor execution.

This entry pilot cannot approve artifacts that did not yet exist. After result
and report are committed, a fresh three-role audit must bind those exact commits,
trees, blobs and digests. The review return must record the actual reviewer-
authored returns or their exact digest-bound substance before admission. Role
names, role templates, expected findings, this entry return or machine-generated
defaults cannot substitute for that fresh input; disagreements and scope warnings
must be preserved.

## 4. Independence and evidence covariance

The only data input for endpoint, class, pair, cross-tab, witness, carrier and
chart reconstruction is the authenticated Git-object content of
`docs/evidence/wp-015d2r-stage-b-v1/result.json` at result commit `d340a2...`.
Other bound files serve only as immutable provenance or parity sources.

The implementation path must be `reconstruction_independent`. It may use Node
standard-library facilities and the generic canonical JSON utility, but it must
not import, invoke, spawn, copy, or transliterate:

- the primary Stage B builder
  `analysis/crpm_world/navigation/assess-wp-015d2r-stage-b-one-next-command-search-v1.ts`;
- the Stage B runner;
- the V4 authority adapter or `shared/simulation.ts`; or
- any route generator, calibration sentinel, D2Q executor, gameplay or hidden
  oracle.

It must not enumerate route words, invoke `createSimulation`,
`applySimulationCommand`, `tickSimulation`, or read ignored raw JSONL as its
answer. The committed endpoint and pair rows are observations to audit; no fresh
domain execution occurs.

The mathematical evidence class remains `correlated_reuse`. Independence of the
reconstruction code improves falsifiability and recoverability; it does not make
the reused Stage B observations empirically independent.

## 5. Fixed audit target

Let `K_WPV4_ONE_COMMAND_COMPLETE_V1(endpoint)` be the exact decoded tuple:

```text
(
  accepted,
  mutated,
  error as null or exact {code, message},
  ordered exact authoritativeEvents,
  post-command movementRemaining
)
```

`K_target` is this tuple over every one of the 274 eligible committed endpoint
occurrences and every unordered pair within an equal
`thin_visible_duel_v0@2` source class. Equality is exact with numeric tolerance
zero. Hashes identify retained bytes but do not substitute for decoded equality.
Route history, revisions and exact state digests are deliberately excluded from
`K_target`.

The world, cut, target, finite domain, committed route records, one-observed-MOVE
horizon, support, tolerance and no-oracle boundary are held fixed. Stage C may
not widen a seed, route, action, cut, horizon or support coordinate.

The D2Q protected core is derived, compared, and never rerun: semantic result digest
`b538f5b6da82dc3b6655a9c0d89404e3b20592191886a9bb4280d6fe481d1615`;
four matched-source-prediction cases (`CMD-VALID-01` accepted and moved,
`CMD-TURN-01` rejected `LATE_TURN`, `CMD-ACTOR-01` rejected
`NOT_YOUR_TURN`, and `CMD-PRECEDENCE-01` rejected `LATE_TURN`); and the
source-bound field verdicts that `actor` and `expectedTurn` are target-relevant
on the declared V4 command gate. These facts are an immutable anchor, not the
alias-producing Stage B observation cut.

Recovery requires strict parsing of the authenticated D2Q return, independent
normalization of all four decoded readouts and both field-verdict rows, and
value-by-value comparison with the registered expectations. Artifact presence,
blob equality, or result-digest equality alone does not satisfy this obligation.
Each case row must retain and compare `caseId`, all request/edge/witness/target
digests, `observedClass`, `inputUnchanged`, `unchangedRejection`, verdict,
source-match, evidence, placement and reopening controls, plus decoded
`accepted`, `mutated`, `error`, ordered events, `eventsDigest`,
`movementRemaining`, `playerX`, `revision`, `preStateDigest` and
`postStateDigest`. The actor/`expectedTurn` rows must retain their full class,
control, frame, evidence, placement and reopening structure. The registered
whole-row digests authenticate the immutable expectation but cannot substitute
for emitting and comparing these decoded values.

## 6. Required independent reconstruction

From the authenticated endpoint records, Stage C must independently:

1. recover exactly 274 eligible endpoints;
2. regroup exact source projections into 9 source classes with sorted sizes
   `1, 1, 8, 8, 29, 29, 61, 61, 76`, of which 7 are aliased;
3. rebuild and uniquely match all 7,378 unordered pair identities;
4. rederive, in precedence order, exactly:

   | Primary outcome | Count |
   | --- | ---: |
   | `frame_support_failure` | 0 |
   | `command_semantic_split` | 1,099 |
   | `continuation_support_split` | 14 |
   | `provenance_exact_state_only_split` | 2,983 |
   | `no_target_relevant_split` | 3,282 |

5. independently reconstruct the ten-row cross-tab by endpoint-depth pair,
   same/cross depth, pre-command budget pair, post-command budget pair and primary
   outcome before comparing it with the Stage B reference digest
   `5247cba5743f97aa05a4e60b4bfed3426d5de2446b1bcdb903e3a518fd64e293`;
6. show that all 1,113 target-relevant pairs have unequal pre-command
   `movementRemaining`; and
7. show that zero target-relevant pairs are same-depth or have equal pre-command
   `movementRemaining`.

The outcome must be derived from decoded records: frame/support failure first,
then command-semantic inequality, then post-command `movementRemaining`
inequality, then pre/post revision or state-digest inequality, else no
target-relevant split. Route inequality alone remains provenance.

Route depth and movement budget must be recorded as co-formed in this finite
result:

| Depth | Pre budget | Post budget | Next MOVE admissible |
| ---: | ---: | ---: | --- |
| 2 | 48 | 40 | yes |
| 4 | 32 | 24 | yes |
| 8 | 0 | 0 | no |

This exact recurrence does not establish causation, unique or global minimality,
all-seed closure, or recursive continuation closure.

## 7. Candidate carrier tests

Three non-vacuous comparisons are mandatory.

The thin-only baseline has 7,378 equal-source pairs and 1,113 `K_target`
counterexamples. It is insufficient for the complete bounded target.

The primary candidate is:

```text
Q_support(state) =
  (thin_visible_duel_v0@2 projection,
   pre-command movementRemaining)
```

It must independently form 17 groups and 6,265 equal-`Q_support` unordered
pairs. It is sufficient only if all 6,265 pairs have equal `K_target`. The exact
expected counterexample count is zero. That yields the narrow verdict
`sufficient_for_complete_registered_one_command_target_on_finite_domain_nonunique`.
It does not yield public formation, recursive state, unique minimality, or a
preferred product carrier.

The narrower command candidate is:

```text
Q_command(state) =
  (thin_visible_duel_v0@2 projection,
   next-MOVE accepted Boolean)
```

Admissibility is the only added coordinate. The candidate must form 14 groups
and 6,279 equal-candidate pairs. It may pass the registered command-semantic
target only when there are zero command-semantic counterexamples. Its 14
continuation-support counterexamples must remain explicit: exact
`movementRemaining` is a separate continuation-support target.

No executor-constructible carrier is called player-public. No public-input plus
declared-support formation witness is introduced.

## 8. Witness and re-entry state

Stage C must derive, not self-attest, four separate dimensions:

1. execution witness present;
2. mathematical evidence class `correlated_reuse`;
3. semantic reconstruction readiness derived in the result;
4. navigator admission, which the result must leave pending;
5. candidate carrier sufficiency from the two implication tests; and
6. Tau licensing, which the result must leave candidate/unlicensed.

On complete success the exact witness semantics are:

- `W_status: present`. `W` is the authenticated complete Stage B execution
  artifact for the registered finite domain. The digest-bound Stage C
  reconstruction certificate is a separate navigator-admission control, not a
  condition retroactively imposed on execution-witness existence. A failed Stage
  C reconstruction blocks admission and `J_W` but does not erase an authenticated
  historical `W`.
- `Omega_W_status: evaluated_exact`. `Omega_W` is the fixed world, thin source
  cut, `K_target`, 274-endpoint domain, retained routes, one-command horizon,
  committed support, tolerance zero and closed oracle/input boundary.
- `P_W_status: observed`. `P_W` contains only the exact positive reconstruction
  facts: counts and identities, outcome/cross-tab match, target constraints, and
  non-vacuous `Q_support` and `Q_command` implications.
- `N_W_status: observed`. `N_W` retains thin-only counterexamples,
  `Q_command` continuation counterexamples, missing same-depth/equal-pre-budget
  positives, covariance and every untested wider/public/recursive claim.
- `J_W: complete_for_bounded_registered_one_command_audit` only when source
  authentication, exhaustive evidence, independent reconstruction, negative
  retention and parity all pass.

At the same time, thin-cut completeness is `intentionally_incomplete`. Audit
completeness never establishes cut completeness, public formation, all-seed
coverage, longer-horizon closure or gameplay adequacy. A failed source, frame,
reconstruction or negative-retention check withdraws the corresponding value;
the implementation must not default all fields to success.

When every semantic control passes, the result emits
`semanticReconstructionReadiness: ready_for_committed_review`,
`navigatorAdmission: pending`, `TAU-WPV4-RETURN-01A: candidate_unlicensed`, and
`chartStatus: candidate_revision_pending_review`. A source-authentication,
frame, reconstruction, negative-retention, witness, or candidate-chart hard-gate
failure stops before either result output is reserved; the failure is retained
in command/test evidence and `WP-015D2U`, but no `not_ready` result/report pair
is published. A distinct review return alone may change admission to
`reviewed_bounded`, Tau to
`licensed_for_bounded_stage_c_reentry_only`, and the chart to
`reviewed_bounded`, after all committed review obligations pass.

## 9. Carrier roles

Every one of these fields receives exactly one primary role from
`live_carrier`, `target_relative_support`, `provenance`, `re_entry_support`, or
`unresolved`, derived under this target and frame:

| Field | Expected role if its exact derivation passes | Guard |
| --- | --- | --- |
| pre-command `movementRemaining` | `live_carrier` | bounded target-relative coordinate in non-vacuous `Q_support`; not public, global, minimal or recursive |
| next-move admissibility | `target_relative_support` | supports command semantics only; does not absorb exact continuation budget |
| route history | `provenance` | exact histories cannot become a vacuous singleton carrier |
| revision | `provenance` | co-formed exact-state proxy; separation alone is insufficient |
| exact state digest | `re_entry_support` | supports exact audit recovery, not a live or preferred carrier |

If a derivation fails, the field becomes `unresolved`; the expected row is not a
labeling shortcut.

The result also derives a compact state-material separation ledger so that one
generic `provenance` label cannot collapse unlike obligations:

- authenticated predecessor bindings plus immutable Stage A/D2Q material are
  `audit_predecessor_provenance`;
- route history and revision are `domain_transition_provenance`;
- the exact state digest is `re_entry_material`;
- `live_recursive_state` is `absent_not_established`; and
- pre-command `movementRemaining` is a bounded one-command live coordinate,
  explicitly not recursive state.

This ledger participates in witness, Tau, chart, result/report parity and
destructive controls. Mutating any category withdraws the corresponding
readiness; it cannot be repaired by a generic provenance label.

## 10. Tau return obligations

`TAU-WPV4-RETURN-01A` begins Stage C as `candidate_unlicensed`, and the machine
result must preserve that status. The result derives semantic readiness only.
Tau becomes `licensed_for_bounded_stage_c_reentry_only` exclusively in the
separate post-result review return if all of the following pass:

- every exact Stage A, Stage B, D2Q, cut and canonical source identity and the
  Stage B semantic result digest authenticate;
- the independent implementation boundary and `correlated_reuse` evidence class
  are preserved;
- every endpoint, class, pair, outcome, cross-tab and target constraint is
  independently reconstructed;
- the four immutable D2Q readouts and both actor/`expectedTurn` field-verdict
  rows are independently parsed, normalized and compared without execution;
- the Stage B command and continuation result remain decodable while carrier,
  provenance and re-entry roles stay split;
- `W`, `Omega_W`, observed `P_W`, observed `N_W` and bounded `J_W` are derived;
- both candidate implication tests and all negative-retention controls pass; and
- the result and report are separately committed with semantic readiness but
  admission pending and Tau candidate/unlicensed;
- committed executable/result/report parity, duplicate controls and the exact
  entry-to-result path/status/mode/blob range pass;
- all three actual pilot returns and the governing synthesis are recorded; and
- chart, residue, reopening and governance controls pass with every
  product/placement/player/gameplay/P5/successor gate closed.

There is no automatic license from a Stage B positive, digest inequality,
revision inequality, pilot agreement, or exact-state recoverability. If any
obligation fails, the review return keeps Tau `candidate_unlicensed` and marks
navigator admission `not_admitted`. Even the narrow licensed state is not
landfall, ProductAuthority, placement, public formation, gameplay authorization,
P5, or successor execution.

## 11. Source-bound chart revision

The new result and report must publish a compact candidate chart revision
pending review, containing:

- strengthened routes supported by exact reconstruction and passed obligations;
- demoted routes, including thin-only for the complete target and
  thin-plus-admissibility for continuation support;
- split carrier roles for budget, admissibility, route history, revision and
  exact digest;
- shoals/dead ends for route-history singleton overfit, revision co-formation,
  exact-state over-thickness, public-formation absence and the one-command limit;
- carried residue and exact reopening conditions; and
- exactly one recommended next transition or an explicit stop.

The candidate chart must demote route history as a live carrier, revision as a co-formed
live-carrier proxy, and exact state digest as an over-thick live carrier. It may
propose strengthening the bounded thin-plus-pre-budget route only after semantic
readiness passes; the review return alone may accept bounded strengthening after
all review obligations pass.

The result must make a conditional choice. It may recommend a separately
preregistered longer-horizon closure test of the returned thin-plus-budget
carrier only if the reconstructed residue and semantic controls select it;
otherwise it records an explicit stop. The review return may accept only that
recommendation or stop. Stage C must not write the successor contract, open its
world, enumerate its domain, or execute it.

## 12. Exact output and validation boundary

Exactly these nine additive regular `100644` paths are allowed from entry HEAD:

### Source outputs

1. `docs/evidence/wp-015d2u.json`
2. `docs/planning/wp-015d2r-stage-c-independent-reconstruction-and-chart-revision-contract-v1.md`
3. `analysis/crpm_world/navigation/wp-015d2r-stage-c-reconstruction-registration-v1.json`
4. `analysis/crpm_world/navigation/reconstruct-wp-015d2r-stage-c-return-v1.ts`
5. `scripts/run-wp-015d2r-stage-c-return-v1.ts`
6. `tests/crpm-world/wp-015d2r-stage-c-return-v1.test.ts`

### Result outputs

7. `docs/evidence/wp-015d2r-stage-c-v1/result.json`
8. `docs/planning/wp-015d2r-stage-c-independent-reconstruction-and-chart-revision-report-v1.md`

### Post-result review-return output

9. `docs/evidence/wp-015d2r-stage-c-v1/review-return.json`

The review-return file must not exist in the source or result commit. It is added
only after the result and report are committed and their exact identities,
semantic digest, parity and entry-to-result range have been audited.

No ignored or raw output path is allowed. The exact lifecycle is:

- entry to source: all six source outputs are `A/100644`; result/report/review
  return remain absent;
- source to result: only result and report are `A/100644`; source outputs remain
  byte-identical and review return remains absent;
- result to review: only review return is `A/100644`; earlier outputs remain
  byte-identical;
- review to terminal: only already-added `docs/evidence/wp-015d2u.json` is
  `M/100644` for final closeout; every other output remains byte-identical; and
- entry to terminal: exactly all nine allow-listed paths are `A/100644`.

At every intermediate and endpoint range, undeclared paths, deletes, renames,
rename-like copies, type changes and nonregular modes are rejected.

Validation must include strict duplicate-key/row/ID/path rejection; Git-object
source authentication; focused and destructive controls; staged parity; exact
changed-path/status/mode/blob auditing from `6e47d42...` through source, result,
review-return and terminal commits; affected bounded canonical validation;
`check:crpm-world-types`; compliance; build; and `git diff --check`.

### Accepted aggregate-suite coverage gap

On 2026-08-30, the complete 32-test Stage C file was run against the frozen
six-file source snapshot. It remained responsive and continuously CPU-bound for
2 hours 35 minutes 53 seconds, with stable memory, but the Node test parent
buffered all TAP until exit and emitted no completed-test results. At the user's
explicit direction the process was then terminated. This aggregate invocation
is `interrupted_no_tap`, not a pass, and no claim may report all 32 tests as
passed.

The user accepted this incomplete aggregate coverage for the present bounded
return. Retained validation evidence consists of: project TypeScript and
work-package checks; independent static/source re-review; registration-boundary
tests 2/2; D2Q/Omega/result destructive controls 6/6; admitted/blocked review
paths 2/2; and the previously executed targeted reconstruction, lifecycle,
range, parity, witness, carrier, Tau and chart controls. Proceeding past this
gap is a documented coordinator waiver, not reconstructed evidence and not a
strengthening of empirical weight. Reopen the aggregate suite if its runtime is
repaired, if any Stage C source byte changes after the source commit, or before
using this result outside the exact bounded Stage C re-entry decision.

Staged parity prevents premature governance claims:

- source parity compares contract, registration, in-progress evidence,
  reconstructor, runner and test;
- result parity compares executable, committed result and report while semantic
  readiness is derived, navigator admission remains pending and Tau remains
  candidate/unlicensed;
- review parity compares committed result/report with the new review return,
  exact range/parity evidence and actual three-role synthesis; and
- terminal parity compares that review return with completed WP-015D2U evidence.

The final evidence cannot self-bind its containing terminal commit; that commit,
evidence blob and exact entry-to-terminal range are verified externally.

Destructive controls must withdraw success when one endpoint, class membership,
pair identity/outcome, cross-tab count, pre-budget relation, target-relevant
constraint, `Q_support` counterexample, retained `Q_command` counterexample,
provenance binding, witness dimension, Tau obligation, chart role, or output path
is mutated. The Stage B runner and historical calibration suite are not Stage C
validation evidence.

## 13. Closed gates and residue

Throughout this pass:

- `ProductAuthority: none`
- `mathematical_placement_implication: none`
- `player_observation_timing: closed`
- `gameplay_change: false`
- `P5: closed`
- `successor_world_execution: closed`

Residue must retain at least: the perfect depth/budget/admissibility/revision
co-formation in this finite source; non-unique finite carrier sufficiency;
unresolved public formation; untested other seeds, routes, actions and longer
horizons; no causal or global-minimality result; no recursive continuation
closure; and no player, timing, UI, browser/device or playtest evidence.

Any changed source byte, input result identity, cut, domain, target, equality,
candidate coordinate, carrier-role rule, Tau obligation, chart rule, output path,
or closed governance gate requires a new version and fresh review before reuse.
