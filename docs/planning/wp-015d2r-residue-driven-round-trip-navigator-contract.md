# WP-015D2R Residue-Driven Round-Trip Navigator — Stage A Contract

Status: Stage A documentation contract complete pending validation and review. Stage B execution is not authorized.

Date: 2026-08-29.

Package: `WP-015D2R`. Child: `WPV4-NAVIGATOR-01A`.

Owner: Worms_Port. Coordinator: the current cross-repository task, sole writer. Parent: immutable [WP-015D2Q return](../evidence/wp-015d2q/review-return.json). Method handoff: CRPM `8c64353f4c8f5504af3e30ca175d9422ca7ae608`. Registry: [frozen planning carrier](wp-015d2r-residue-driven-round-trip-candidate-registry.json). Evidence: [WP-015D2R record](../evidence/wp-015d2r.json).

## Authorization And Operating Cut

The owner authorized only Stage A from the CRPM/Worms handoff: bind the sources, reconstruct the protected family and residue, freeze an incomplete transformation chart, select one question-forming transition, and stop before execution. This child is documentation-only. It does not run a pressure case, search a route, replay D2Q as new evidence, change gameplay, or open Stage B or Stage C.

`pilot_activation: not_required`: this pass forms one Worms-owned L4+ question. It neither accepts nor transfers a material cross-world result and issues no placement, promotion, ProductAuthority, or P5 verdict. A future material Stage C return must reassess the full CRPM pilot gate.

The current CRPM method source and Worms result are deliberately asymmetric:

- CRPM `c9bf511f67ffbe518cd9f9603916bb26e1527736` contains the historical D2P documentation-coordination receipt. It is not a D2Q return.
- Worms_Port `4fc88d947a98d737cbcdfd22d31d36c4bd5843eb` contains the terminal D2Q result.
- CRPM `8c64353f4c8f5504af3e30ca175d9422ca7ae608` adds the later Worms navigator handoff. It is the current Stage A handoff source, not a rewrite of the earlier receipt.
- D2Q executable source `85f6ea945bd2b5b2aacb6b61cc2af52fe0c09ebb` and result digest `b538f5b6da82dc3b6655a9c0d89404e3b20592191886a9bb4280d6fe481d1615` remain immutable.

All CRPM paths are read-only method or predecessor sources. Worms_Port owns the target, world realization, evidence meaning, product boundary, and any later execution. No CRPM code, prose, schema, or runtime dependency is imported.

## Air — Existing Threads And Open Field

The repository does not lack a generic navigator. The existing Unknown Source Ocean bundle already relates cut choice, witness/probe/carrier distinctions, transition, lookback, residue, chart revision, and recoverable landfall. This child therefore creates a Worms-specific adapter contract, not a parallel CRPM framework, schema, or engine.

The useful source threads have separate jobs:

| Thread | Stage A use | Explicit limit |
| --- | --- | --- |
| canonical `CutTransition` | type the proposed movement between charts | only `refine`, `compress`, `decompress`, `reorganize`, and `overlap-move` are primitive |
| witness-sufficiency interface | expose witness, observation, boundary, target, null-space, support, and sufficiency status | L4+ question grammar, not Worms evidence |
| P2 durable return | keep adequacy, public formation, lifecycle, provenance, and expiry obligations distinct | no field is permanent merely because it helped one audit |
| P3 durable return | keep reconstruction path, evidence covariance, and successor horizon explicit | re-entry does not multiply empirical weight |
| Unknown Source navigator | retain failed routes, probe wake, residue-selected succession, and false-landfall controls | candidate set and route set remain incomplete |
| D2P MOVE/RETURN families | identify the nearest authority-side realization basin | rows remain documentation questions, not execution authorization |
| D2Q | provide a bounded command-gate chart and live residue | not the protected gameplay target or a global carrier verdict |

The wider protected family remains the one in the [D2G navigation review](wp-015d2g-relational-gameplay-navigation-review.md): visible commitment and credible response, inspectable local reorganization without free reset, convergence, opening/obligation/timing visibility, initiative indexed by carrier and route, distinct Relic roles, production spawn 640, and the D2A ideal-direct cut boundary. Stage A does not claim that its first authority-side subcore satisfies that family.

## Fire — One Bounded Target

Stage A selects only:

```text
K_WP_ONE_NEXT_COMMAND_RETURN:
  Under unchanged V4 authority, determine whether equality under one declared
  thin authority projection preserves the exact one-next-command continuation
  readout for naturally reached movement prefixes after route, actor, turn,
  support, horizon, tolerance, and oracle boundary are held fixed.
```

This is a bounded `T_RETURN_COORDINATES` subcore. Its protected readout is command acceptance, mutation, exact error or null, ordered authoritative events, post-state digest, revision, and `movementRemaining` after one declared next command.

“Credible response” is not part of this child target. It remains residue because it needs player-visible information and timing authority. Aim/fire, projectile resolution, simulation ticks, wall-clock time, networking, UI, player observation, gameplay balance, ProductAuthority, and placement are also excluded.

## Source Boundary

The [planning registry](wp-015d2r-residue-driven-round-trip-candidate-registry.json) is the machine-readable source of truth for exact paths, commits, blobs, Git-byte SHA-256 values, roles, lineage, target, candidate rows, selected transition, support, and authorization state.

The source binder uses committed Git bytes. For the new CRPM handoff, that is:

```text
commit: 8c64353f4c8f5504af3e30ca175d9422ca7ae608
blob: efad460594428c8a2c12a391486af7cc1e460e4a
git_blob_sha256: 715866958180e34e0df5a1bc09a507d320b2913da713330cd57173e32266d903
```

The Windows checkout hash differs because line-ending conversion is configured. It is not the canonical source digest. Stage A also binds the canonical five-kind transition source, the existing navigator index, the witness interface, durable P2 and P3 returns, the placement-unit owner boundary, the D2G protected family, D2P question registry, D2Q return, movement calibration, and Worms operational governance. Goal packets are lineage; durable P2/P3 returns are predecessor facts.

No historical source pin rotates. The new `legal/source-manifest.json` row is additive. Historical validators are not run from this descendant HEAD when they own an exact older range; predecessor identity is verified through committed objects instead.

## Immutable D2Q And Retrospective Calibration

D2Q earned only the following chart:

- actor matters on the matching-turn stratum;
- the turn-mismatch pair retains an actor no-witness control;
- expected turn has earlier guard precedence in the both-invalid case;
- equal rejected post-states can carry different exact errors;
- accepted movement changes events, position, movement budget, and revision; and
- evidence is `correlated_reuse` with disposition `structural_reference`.

Nothing in Stage A converts the observed D2Q movement-budget change into carrier necessity.

The earlier one-versus-four right/left-cycle pair is a separate retrospective calibration sentinel. At seed `3237998097`, both naturally reached prefixes agree under the declared thin visible projection while budgets `48` and `0`, revisions `2` and `8`, and the same next-right-move result split. It is useful to verify later machinery but cannot count as a fresh selected witness, empirical independence, wider `K_WP` satisfaction, or evidence that the fields are player-public.

If Stage B is later authorized, a positive search must use a preregistered finite domain and an eligible holdout pair outside that calibration sentinel. The sentinel and D2Q may generate and check the hypothesis; they cannot certify a search that selected them.

## Frozen Incomplete Candidate Chart

The registry identity is `WP-015D2R:stage-a-transformations:v0`. It is a manual planning carrier, not an executable registry or schema. `declared_complete` and `execution_registration` are both false.

| Candidate | Primitive | Stage A status |
| --- | --- | --- |
| `TAU-WPV4-RETURN-01A` | `reorganize` | selected for contract only |
| movement-support omission | `compress` | historical calibration already exposes one split |
| actor/turn stratum refinement | `refine` | source-supported by D2Q, not selected |
| trace-to-carrier formation | composition around `reorganize` | support-dependent; public formation underdetermined |
| authority-time refinement | `refine` | blocked source/domain; TIME lane closed |
| player-response overlap | `overlap-move` | blocked source/domain; PUBLIC lane closed |

The wider exploratory transition names `branch_change`, `interface_bridge`, and `recombine` are not imported as primitives. The canonical five-kind grammar controls this contract.

## Selected Transition Packet

```text
transition_id: TAU-WPV4-RETURN-01A
type: reorganize
C_from: C_D2Q_COMMAND_GATE
C_to: C_ONE_NEXT_COMMAND_CONTINUATION
reversibility: protected-equivalent, declared but not earned
```

Fixed frame:

- **world:** unchanged `nimble-knots-artillery-v4` authority;
- **cut:** thin authority equality versus one-next-command continuation-bearing return;
- **target:** `T_RETURN_COORDINATES/K_WP_ONE_NEXT_COMMAND_RETURN`;
- **domain:** a future exact finite set of naturally reached accepted-movement prefixes, presently unbound;
- **routes:** legal V4 commands only, with the historical calibration sentinel excluded from fresh-validation count;
- **horizon:** one next command, no tick advancement;
- **support:** exact ruleset/seed, full initialization and legal prefix, actor, expected turn, movement budget, revision, route provenance, and unchanged authority;
- **tolerance:** exact canonical equality; and
- **oracle/input boundary:** authority-readable state may establish natural reachability and target output, but case IDs, expected verdicts, selected-pair labels, and reviewer knowledge cannot enter class or selection keys.

Preserved:

- the immutable D2Q chart and conditional scope;
- exact V4 command, error, event, and authority semantics;
- the wider WP-015D2 protected family as an unsatisfied boundary; and
- route, support, source, and covariance provenance.

Forgotten by the target chart, while retained as audit provenance:

- D2Q case identifiers as live continuation coordinates;
- any implication that command-gate coverage alone answers `K_WP`; and
- post-state-only equality as a sufficient command/return readout.

Newly visible:

- the natural prefix and ordered movement route;
- budget and revision at the return endpoint;
- exact one-next-command continuation equality or split; and
- support debt between authority response and wider gameplay response.

Residual:

- credible player response and initiative;
- aim/fire, projectile and time behavior;
- player-public observation and formation;
- global carrier minimality, gameplay authority, placement, and P5.

Protected-equivalent reversibility is only a declared return obligation. It is earned only if a future return recovers the D2Q protected readout from immutable provenance while preserving the new continuation result and residue.

## Predicted Signature, Falsifier And Search Discipline

Predicted natural-world signature:

> Outside the known calibration sentinel, two naturally reached legal prefixes alias under the declared thin projection but differ under the exact one-next-command continuation readout while the fixed frame remains unchanged.

Negative witness:

> A preregistered exhaustive finite search finds no eligible holdout pair, or every apparent split disappears when the target, route, horizon, support, tolerance, and oracle boundary are fixed.

A negative result must demote or split this route and remain chart residue. Stage B may not adaptively widen its domain, choose a pair after inspecting outcomes, inject state, add an undeclared oracle, or count the calibration sentinel as a new discovery. Its future contract must freeze enumeration, failure retention, stopping rule, holdout or reconstruction role, exact output allow-list, and historical controls before execution.

Natural reachability and target relevance remain distinct. A reachable pair can be non-discriminating; a target-relevant manufactured pair is inadmissible.

## Witness And Support Boundary

The registry fills `W`, `O_W`, `B_W`, `Omega_W`, `K_target`, `P_W`, `N_W`, `S_W`, and `J_W`. `J_W` is `incomplete` because the future world witness and its exact domain are absent.

Authority-readable support includes full simulation state, `movementRemaining`, revision, exact events/errors, and route history. This does not establish player visibility or public formation. Both remain `underdetermined` until their own Lane 4 boundary is authorized and witnessed.

Evidence covariance remains `correlated_reuse`. D2Q, historical movement tests, source documents, deterministic rechecks, this contract, and reviewers reuse related source facts. They improve traceability and falsifiability, not empirical weight.

## Round Trip And Return Test

Stage A declares but does not perform:

```text
C_D2Q_COMMAND_GATE
  --reorganize--> C_ONE_NEXT_COMMAND_CONTINUATION
    --future legal V4 voyage--> returned world trace
      --future compression/overlap return--> D2Q-comparable protected core
```

The future comparison must report `survived`, `lost`, `gained`, `vanished`, `residual`, and `support_debt` separately. It may not replace them with a scalar score. Current values are empty or `not_tested`; `landfall_status` is `not_evaluated`; `closure_allowed` is false; no post-result successor is selected.

## Files And Validation

Only these seven paths may change across the Stage A range:

1. `docs/evidence/wp-015d2r.json`;
2. `docs/evidence/wp-015d2r/review-return.json`;
3. `docs/planning/wp-015d2r-residue-driven-round-trip-navigator-contract.md`;
4. `docs/planning/wp-015d2r-residue-driven-round-trip-candidate-registry.json`;
5. `docs/planning/implementation_plan.md`;
6. `docs/planning/wp-015d2g-relational-gameplay-navigation-review.md`; and
7. `legal/source-manifest.json`.

The two existing planning files receive insertion-only discoverability notes. The source manifest receives one additive CRPM record; all old records remain byte-for-byte equivalent as parsed objects. The other four files have distinct contract, registry, work-evidence, and compact-return lifecycles.

Validation must:

1. run the compliance funnel before and after documentation;
2. strictly reject duplicate JSON keys, source IDs, candidate IDs, omitted or extra candidate rows, and inconsistent selection;
3. reproduce every committed source path/blob/Git-byte SHA and preserve D2P/D2Q and historical manifest objects;
4. compare contract, registry, work evidence, and return for package/child IDs, target, selected transition, source boundary, authorization, evidence class, residue, reopening condition, and exact allowed outputs;
5. audit the complete range from `4fc88d947a98d737cbcdfd22d31d36c4bd5843eb`, plus every intermediate commit, for exact paths, A/M status, regular mode `100644`, and blobs;
6. run working and staged whitespace checks; and
7. obtain identified read-only documentation and boundary review.

Build, type, simulation, tactical, CRPM-world, browser/device, playtest, and dependency-audit checks are deliberately skipped because no code, schema, dependency, runtime, or gameplay surface changes. No D2Q pressure executor or historical HEAD-sensitive validator is run from this descendant branch.

## First Governed Stop

Stage A stops when the new contract, frozen incomplete registry, work evidence, source manifest entry, discoverability notes, and source-bound review return are committed and the terminal seven-path audit is externally reported.

At that stop:

- Stage B realization search is `not_authorized`;
- Stage C review/return is `not_authorized`;
- the active Execution Pointer remains WP-015D2A;
- D2O's player-information/timing/response gate remains unsatisfied;
- ProductAuthority and mathematical placement implication remain `none`; and
- P5 remains closed.

A changed source, target, cut, candidate set, route domain, horizon, support, oracle/access boundary, owner decision, or missing recoverable artifact requires a new source-bound review. Stage B opens only through separate explicit owner authorization for one exact candidate and domain.
